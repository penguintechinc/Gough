apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "worker-ipxe.fullname" . }}-config
  labels:
    {{- include "worker-ipxe.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
