apiVersion: v1
kind: Secret
metadata:
  name: {{ include "worker-ipxe.fullname" . }}-secret
  labels:
    {{- include "worker-ipxe.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
