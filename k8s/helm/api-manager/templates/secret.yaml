apiVersion: v1
kind: Secret
metadata:
  name: {{ include "api-manager.fullname" . }}-secret
  labels:
    {{- include "api-manager.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
