apiVersion: v1
kind: Secret
metadata:
  name: {{ include "discovery-agent.fullname" . }}-secret
  labels:
    {{- include "discovery-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
