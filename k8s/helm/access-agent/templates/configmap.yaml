apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "access-agent.fullname" . }}-config
  labels:
    {{- include "access-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
