apiVersion: v1
kind: Secret
metadata:
  name: {{ include "access-agent.fullname" . }}-secret
  labels:
    {{- include "access-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
